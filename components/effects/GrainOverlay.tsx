"use client";

/**
 * Grain. An inline SVG-noise data URI at very low opacity, fixed over
 * everything so the whole room shares one film. No asset file.
 */
export default function GrainOverlay() {
  return <div className="grain" aria-hidden="true" />;
}
